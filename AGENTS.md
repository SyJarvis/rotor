# AGENTS.md

**取舍：** 以下原则偏向谨慎而非速度。对非常简单的任务，自行判断。

## 核心原则

1. **先思考再编码** — 不要假设，不要掩盖困惑，明确呈现取舍
2. **简洁优先** — 用最少代码解决问题，不做投机性扩展
3. **精准修改** — 只改必须改的，只清理自己造成的混乱
4. **目标驱动** — 定义可验证的成功标准，循环推进直到验证完成

详细指南见对应 skill。

## Skills

遇到以下场景时，按需加载对应的 `.agents/skills/` 中的 skill：

| 场景 | Skill |
|------|-------|
| 编码前行为准则、代码风格、重构取舍 | `coding-guidelines` |
| 编写单元/集成/回归/异步测试 | `test-writer` |
| 识别并安全移除死代码 | `clean-dead-code` |
| 创建新 skill 或更新已有 skill | `skill-creator` |