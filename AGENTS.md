# AGENTS.md

**核心取舍**：优先保证正确性、可验证性与代码演进质量，而非执行速度。

---

## 1. 核心原则

* **极简与演进（KISS / YAGNI / DRY）**
  * 优先选择最简单、最容易验证的方案。不做预测性设计或过度抽象。
	* **原则仲裁**：当原则发生冲突时，**KISS 和 YAGNI 永远优先于 DRY**. 严禁为了消除几行重复代码而引入复杂的抽象接口或泛型.
	* **阻塞性重构限制**：仅当历史遗留代码直接物理阻塞当前任务实现时，才允许进行重构. 必须遵循“重构两步法”——**先独立重构并验证，再添加新功能**. 严禁顺手修复不相关的代码坏味道（Code Smells）.
* **事实优先（Ground Truth）**
  * 必须先通过工具检索相关代码、配置与测试，禁止依据凭空猜测判断接口行为或项目结构。
  * 找不到依据时明确说明“不确定”，严禁瞎编。
* **精准控制影响域（Blast Radius）**
  * 修改必须保持高内聚，禁止顺手调整无关代码的格式或规范。
* **反馈驱动与止损（Feedback Protocol）**
  * 修改前定义成功标准，修改后必须通过客观手段（测试/Lint/日志）验证，禁止使用“逻辑上正确”代替实际验证.
  * **三次失败协议**：同一任务或者同一个报错连续 2 次修改并验证失败后，必须立即停止盲目试错，回滚临时代码，并向人类暴露详细的卡点（Blocker）分析与完整错误日志.

---

## 2. 主代理与子代理分工

* **主代理（Tech Lead）**：
  * **职责**：理解目标、检索上下文、拆解计划、审查子代理结果与验证日志、汇报最终产出。
  * **许可**：可执行只读或临时探针验证（Spike Solution）以确认技术可行性。
  * **禁止**：不得直接修改或提交生产业务代码、最终测试用例及文档。
* **子代理（Executor）**：
  * 必须基于明确的目标、上下文和可验证标准执行任务。
  * 交付时必须提供：**实际修改 Diff**、**事实依据**、**验证命令与日志**、**遗留风险或潜在影响域**。
  * 主代理必须审查实际证据，若未检测到完整的验证日志，必须无条件退回要求子代理重做。

---

## 3. 标准工作流

`搜索事实` → `方案设计与 DoD（完成标准） 定义` → `原子任务拆解` → `委派子代理` → `局部执行与增量验证` → `审查 Diff 与日志` → `全局集成验证` → `汇报结果`

---

## 4. 提交与 PR 规范

1. **原子化提交**：一个 PR/ Commit 只解决一个独立问题，保持最小可审查 Diff，不混入无关重构。
2. **证据驱动验收**：提交前必须跑通全套关联测试与静态检查，未执行客观验证不得声称已通过.
3. **事实描述**：提交描述需包含变更原因、修改方案及实际验证的客观结果，不把推测写成事实.
4. **物理去痕迹化**：保持人类代码库的绝对纯净. 严禁提交临时调试代码. **严禁在 Commit Message、代码注释及文档中添加 `Co-Authored-By`、`Generated by AI` 或任何暴露大模型特征的署名与解释性废话.**
